version: "2.1"
volumes:
  files:
services:
  file-block:
    build:
      context: .
    restart: always
    volumes:
      - 'files:/data'
    #image: ghcr.io/balena-io-playground/cron-block:latest
    ports:
      - "80:3000"
